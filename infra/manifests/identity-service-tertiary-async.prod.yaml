---
apiVersion: v1
kind: Namespace
metadata:
  name: microservices
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service-tertiary-async
  namespace: microservices
spec:
  selector:
    app: identity-service-tertiary-async
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: identity-service-tertiary-async
  namespace: microservices
  annotations:
    kubernetes.io/ingress.class: "tectonic"
    ingress.kubernetes.io/rewrite-target: /
spec:
  tls:
  - hosts:
      - identity.beyondthehype.pl
  rules:
  - host: identity.beyondthehype.pl
    http:
      paths:
      - backend:
          serviceName: identity-service-tertiary-async
          servicePort: 80
        path: /
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: identity-service-tertiary-async
  namespace: microservices
spec:
  replicas: 1 #todo replicas
  template:
    metadata:
      labels:
        app: identity-service-tertiary-async
        monitoring-group: microservices-metrics
    spec:
      nodeSelector:
        nodetype: microservices
      containers:
        - name: identity-service-tertiary-async
          image: quay.io/virtuslab/identity-service-tertiary-async
          resources:
            limits:
              memory: "384Mi"
          ports:
            - containerPort: 8080
            - containerPort: 8081
          env:
            - name: http_port
              value: "8080"
            - name: CASSANDRA_CONTACT_POINT
              value: "cassandra-0.cassandra.databases.svc.cluster.local"